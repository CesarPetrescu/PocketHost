name: ci

on:
  push:
  pull_request:

jobs:
  go-and-repo-checks:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-go@v5
        with:
          go-version: '1.23.x'
      - run: ./scripts/ci-local.sh
