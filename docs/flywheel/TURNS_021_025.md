# Turns 021-025

## Turn 022 - bundled cloudflared Android source build

- Change: packaged `cloudflared` Android artifacts for `arm64-v8a`, `x86`, and `x86_64`; added `scripts/build-cloudflared-android.sh`; recorded upstream version, source commit, license, and artifact hashes in `NOTICE`; fixed script executable modes so `./scripts/ci-local.sh` works as documented.
- Human gate: user explicitly confirmed cloudflared usage before bundling the third-party binary.
- Blast radius: APK native-library payload, Cloudflare docs, license/notice metadata, and build scripting. No tunnel credentials, tokens, hostnames, certs, or default public routes were added.
- Data flow: Android supervisor launches `libcloudflared.so tunnel --config <app files>/config/cloudflared.yml run`; `cloudflared` makes outbound tunnel connections based only on the operator-supplied app-private config. Missing config remains a configuration failure before process launch.
- Source evidence: upstream `cloudflare/cloudflared` tag `2026.5.2`, commit `0e84636de9450d9e73c1e28932ed2bd62cb33e10`, Apache-2.0 license.
- Build evidence: `./scripts/build-cloudflared-android.sh /tmp/cloudflared-2026.5.2 all` produced Android PIE executables with SHA256 `6a4b355fb8af5ab6603f2e18ee1c656315477a1c6dd7b89165e0810e8bfa9630` for arm64-v8a, `60be4cbe2299d8f9a060f0c99bfa611d537e43a4c5340278063267597d26f53d` for x86, and `e77f2c2463262c23f2c238fd904838eeec4afa0cacb3afce8c5f8e387bda7494` for x86_64.
- Verification: `cd go && go test ./...` passed; `bash scripts/verify-daemons-local.sh` passed; `./scripts/ci-local.sh` passed; `bash scripts/package-android.sh` produced `android/app/build/outputs/apk/debug/app-debug.apk`; APK contents include `libcloudflared.so` for `arm64-v8a`, `x86`, and `x86_64`; installed the APK on `emulator-5554` with `primaryCpuAbi=x86_64`; `/data/app/.../lib/x86_64/libcloudflared.so --version` returned `cloudflared version 2026.5.2`; foreground supervisor notification id `4401` was visible; `hostd` and `webd` health passed through ADB-forwarded loopback ports.
- Tunnel evidence: not run in this turn because the repo contains no Cloudflare tunnel credentials and no public route was approved or configured.
- Known gaps: no tunnel credentials are present, no public hostname was verified, and no long-running tunnel stability data exists yet.
- Rollback: remove `libcloudflared.so` from each `jniLibs` ABI directory, revert `NOTICE`/Cloudflare docs, and keep the Android service slot as a missing-binary optional integration.
